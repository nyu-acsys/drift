(* ============================================================================================ *)
(* ===                         III. 3-State AUTOMATON & Initial CFG                         === *)
(* ============================================================================================ *)
{ 
	QSet   = [0; 1; 2; 3]; 
  	delta  = fun evx (q, acc) -> 
	           if (q = 0) then (1, acc) 
			 else if (q = 1) then (2, acc) 
			      else if (q = 2) then (3, acc)
                 	      	   else if (q = 3) then (3, acc) 
				   	else ((-1), acc); 
	assert = fun (q, acc) -> q > 0; 
 	IniCfg = (0, 0) 
}
