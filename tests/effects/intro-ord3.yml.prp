QSet = [0; 1; 2];

delta = fun evx (q, acc) ->
          if (q = 0) then (1, evx)
	  else if (q = 1 && evx >= acc) then (1, acc)
	  else (2, acc);
	  
IniCfg = (0, 0);

assert = fun (q, acc) -> q < 2;
